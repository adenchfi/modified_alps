get_filename_component(SELF_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

set(ALPS_MPS_BUILD_SYMMETRIES @DMRG_BUILD_SYMMETRIES@)
set(ALPS_MPS_DMRG_NUMSYMM @DMRG_NUMSYMM@)

set(ALPS_MPS_INCLUDE_DIRS @CONF_INCLUDE_DIRS@)
set(ALPS_MPS_LIBRARY_DIRS @CONF_LIBRARY_DIRS@)
set(ALPS_MPS_LINKER_FLAGS "@ALPS_MPS_LINKER_FLAGS@")
set(ALPS_MPS_DEFINITIONS @ALPS_MPS_DEFINITIONS@)

set(ALPS_MPS_SYMMETRY_CMAKE @CONF_SYMMETRY_CMAKE@)

# Our library dependencies (contains definitions for IMPORTED targets)
if(NOT TARGET mps_models AND EXISTS "${SELF_DIR}/ALPSMPS-targets.cmake")
  include("${SELF_DIR}/ALPSMPS-targets.cmake")
endif()
 
set(ALPS_MPS_LIBRARIES @CONF_LIBRARIES@)
